SELECT flight_id, flight_on, status FROM flights
WHERE status = "on time";

SELECT * FROM bookings WHERE total_amount >= 1000000;

SELECT * FROM aircrafts_data;

SELECT flight_id, flight_on, FROM flights 
WHERE aircrafts_code = "733";

SELECT * FROM tikets 
WHERE passenger_name = "IRINA%";